{% extends '::base.html.twig' %}

{% block toolbar %}
    <a class="left" data-toggle="side-menu">
        <i class="fa fa-bars"></i>
    </a>
    <a class="right" data-toggle="remove">
        Suppr
    </a>
    Catégories
{% endblock %}

{% block body %}
    <form class="search-form" action="{{ path('ferus_categories_new') }}" method="get">
        <input type="text" placeholder="Nouveau" name="name">
    </form>
    <ul>
        {% for category in categories %}
            <li data-remove="lol">
                <a href="{{ path('ferus_categories_show', {id: category.id}) }}">
                    {{ category }}
                    <i class="fa fa-chevron-right"></i>
                </a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}